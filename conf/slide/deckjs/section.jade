if attr["canvas-image"]
  section(id=id,class=["slide","canvas-image","#{role}"],style="background-image: #{attr['canvas-image']}")
    !{content}
else
  section.slide(id=id,class=["#{role}"])
    if has_role.title
      h1 !{title}
    else
      h2 !{title}
    !{content}
