div.audioblock(id=id,class=[style, role])
    if title
        div.title !{captioned_title}
    div.content
        audio(src=attr.target,autoplay=option.autoplay || "",controls=option.nocontrols || "",loop=option.loop || "")
            | "Your browser does not support the audio tag."